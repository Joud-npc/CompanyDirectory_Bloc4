<Window x:Class="CompanyDirectory.Views.AdminPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Accès Administrateur" Height="300" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="🔒" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="Mode Administrateur" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Text="Veuillez saisir le mot de passe administrateur" 
                       FontSize="12" HorizontalAlignment="Center" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Saisie mot de passe -->
        <StackPanel Grid.Row="1" Margin="0,10">
            <TextBlock Text="Mot de passe :" Margin="0,0,0,8" FontWeight="SemiBold"/>
            <PasswordBox x:Name="PasswordBox" 
                         Height="35" 
                         FontSize="14"
                         Margin="0,0,0,10"
                         Background="White"
                         BorderBrush="Gray"
                         BorderThickness="1"
                         Padding="8,5"
                         KeyDown="PasswordBox_KeyDown"/>
        </StackPanel>

        <!-- Message d'erreur -->
        <TextBlock Grid.Row="2" 
                   x:Name="ErrorMessage" 
                   Foreground="Red" 
                   FontSize="11" 
                   Margin="0,5,0,0" 
                   Visibility="Collapsed"
                   TextWrapping="Wrap"
                   VerticalAlignment="Top"/>

        <!-- Boutons -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,20,0,0">
            <Button x:Name="BtnValidate" 
                    Content="Valider" 
                    Width="90" 
                    Height="35" 
                    Click="BtnValidate_Click" 
                    IsDefault="True" 
                    Margin="0,0,10,0"
                    Background="#007ACC"
                    Foreground="White"
                    BorderThickness="0"
                    FontWeight="SemiBold"/>
            <Button x:Name="BtnCancel" 
                    Content="Annuler" 
                    Width="90" 
                    Height="35" 
                    Click="BtnCancel_Click" 
                    IsCancel="True"
                    Background="#E1E1E1"
                    Foreground="Black"
                    BorderThickness="1"
                    BorderBrush="Gray"/>
        </StackPanel>
    </Grid>
</Window>