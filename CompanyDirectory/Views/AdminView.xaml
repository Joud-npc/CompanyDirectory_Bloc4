<Window x:Class="CompanyDirectory.Views.AdminView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:CompanyDirectory.ViewModels"
        xmlns:converters="clr-namespace:CompanyDirectory.Converters"
        Title="Admin - Gestion Employés" Height="500" Width="900"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:AdminViewModel />
    </Window.DataContext>

    <DockPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="10">
            <Grid>
                <TextBox x:Name="SearchTextBox" Width="250" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="Nom / Prénom / Mail" IsHitTestVisible="False" Foreground="Gray" Margin="5,0,0,0" VerticalAlignment="Center"
                           Visibility="{Binding Text, ElementName=SearchTextBox, Converter={StaticResource StringToVisibilityConverter}}" />
            </Grid>

            <Button Content="Rechercher" Width="100" Margin="5" Command="{Binding SearchCommand}" />
            <Button Content="Ajouter" Width="100" Margin="5" Command="{Binding AddCommand}" />
            <Button Content="Modifier" Width="100" Margin="5" Command="{Binding EditCommand}" />
            <Button Content="Supprimer" Width="100" Margin="5" Command="{Binding DeleteCommand}" />
        </StackPanel>

        <DataGrid ItemsSource="{Binding Employees}" SelectedItem="{Binding SelectedEmployee}" AutoGenerateColumns="False" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom" Binding="{Binding LastName}" />
                <DataGridTextColumn Header="Prénom" Binding="{Binding FirstName}" />
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" />
                <DataGridTextColumn Header="Téléphone" Binding="{Binding Phone}" />
                <DataGridTextColumn Header="Site" Binding="{Binding Site}" />
                <DataGridTextColumn Header="Service" Binding="{Binding Service}" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
