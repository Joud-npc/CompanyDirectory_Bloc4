<Window x:Class="CompanyDirectory.Views.AdminView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:vm="clr-namespace:CompanyDirectory.ViewModels"
        Title="Administration" Height="700" Width="1100" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:AdminViewModel />
    </Window.DataContext>

    <Grid>
        <DockPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="8">
                <Button Content="Charger" Command="{Binding LoadCommand}" Margin="0,0,8,0"/>
                <Button Content="Ajouter" Command="{Binding AddCommand}" Margin="0,0,8,0"/>
                <Button Content="Modifier" Command="{Binding EditCommand}" Margin="0,0,8,0"/>
                <Button Content="Supprimer" Command="{Binding DeleteCommand}" Margin="0,0,8,0"/>
            </StackPanel>

            <DataGrid ItemsSource="{Binding Employees}" SelectedItem="{Binding SelectedEmployee}" AutoGenerateColumns="False" Margin="8" IsReadOnly="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding Id}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Nom" Binding="{Binding LastName}" />
                    <DataGridTextColumn Header="Prénom" Binding="{Binding FirstName}" />
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" />
                    <DataGridTextColumn Header="Téléphone" Binding="{Binding Phone}" />
                    <DataGridTextColumn Header="Mobile" Binding="{Binding MobilePhone}" />
                    <DataGridTextColumn Header="SiteId" Binding="{Binding SiteId}" />
                    <DataGridTextColumn Header="ServiceId" Binding="{Binding ServiceId}" />
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</Window>