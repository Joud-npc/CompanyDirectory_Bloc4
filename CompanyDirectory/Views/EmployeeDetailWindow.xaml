<Window x:Class="CompanyDirectory.Views.EmployeeDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fiche Employé" Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <Border Grid.Row="0" Background="LightBlue" Padding="15" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock x:Name="TxtFullName" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock x:Name="TxtPosition" FontSize="14" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Détails -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="35"/>
            </Grid.RowDefinitions>

            <!-- Nom -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Nom :" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TxtLastName" VerticalAlignment="Center"/>

            <!-- Prénom -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Prénom :" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TxtFirstName" VerticalAlignment="Center"/>

            <!-- Email -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Email :" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Grid.Row="2" Grid.Column="1" x:Name="TxtEmail" VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="TextDecorations" Value="Underline"/>
                                <Setter Property="Foreground" Value="Blue"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- Téléphone fixe -->
            <TextBlock Grid.Row="3" Grid.Column="0" Text="Téléphone :" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Grid.Row="3" Grid.Column="1" x:Name="TxtPhone" VerticalAlignment="Center"/>

            <!-- Site -->
            <TextBlock Grid.Row="4" Grid.Column="0" Text="Site :" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Grid.Row="4" Grid.Column="1" x:Name="TxtSite" VerticalAlignment="Center"/>

            <!-- Service -->
            <TextBlock Grid.Row="5" Grid.Column="0" Text="Service :" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Grid.Row="5" Grid.Column="1" x:Name="TxtService" VerticalAlignment="Center"/>
        </Grid>

        <!-- Boutons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="BtnGeneratePdf" Content="Générer PDF" Width="100" Height="30" 
                    Click="BtnGeneratePdf_Click" Margin="0,0,10,0"/>
            <Button x:Name="BtnClose" Content="Fermer" Width="80" Height="30" 
                    Click="BtnClose_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>