<Window x:Class="CompanyDirectory.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:vm="clr-namespace:CompanyDirectory.ViewModels"
        Title="Connexion" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:LoginViewModel />
    </Window.DataContext>
    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Identifiant" />
            <TextBox x:Name="UsernameBox" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="Mot de passe (démo: user)" Margin="0,10,0,0" />
            <PasswordBox x:Name="PasswordBox" />
            <Button Content="Se connecter" Margin="0,10,0,0" Padding="6"
                    Click="Connect_Click" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" />
        </StackPanel>
    </Grid>
</Window>